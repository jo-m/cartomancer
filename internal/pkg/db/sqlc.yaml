version: "2"
sql:
  - engine: "sqlite"
    queries: "queries"
    schema: "migrations"
    gen:
      go:
        package: "db"
        out: "."
        emit_db_tags: false
        emit_prepared_queries: false
        emit_json_tags: true
